{
  "name": "latex-citation-stats",
  "displayName": "LaTeX Citation Stats",
  "description": "Real-time citation manager and tracker for LaTeX. Fully offline.",
  "license": "MIT",
  "version": "1.1.0",
  "publisher": "ActiveClientMods",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ActiveClientMods/latex-citation-stats.git"
  },
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:**/*.bib",
    "workspaceContains:**/*.tex"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "latexCitationStats",
          "title": "LaTeX Citations",
          "icon": "media/citation.svg"
        }
      ]
    },
    "views": {
      "latexCitationStats": [
        {
          "id": "latexCitationStats.view",
          "name": "Citations",
          "icon": "media/citation.svg",
          "contextualTitle": "LaTeX Citations"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "latexCitationStats.view",
        "contents": "No BibTeX entries found yet.\nOpen a workspace containing .bib and .tex files to see your citation usage here."
      }
    ],
    "commands": [
      {
        "command": "latex-citation-stats.refresh",
        "title": "Refresh Citations",
        "category": "LaTeX Citations",
        "icon": "$(refresh)"
      },
      {
        "command": "latex-citation-stats.goToUsage",
        "title": "Go to Usage",
        "category": "LaTeX Citations",
        "icon": "$(go-to-file)"
      },
      {
        "command": "latex-citation-stats.goToBibDefinition",
        "title": "Go to Bib Definition",
        "category": "LaTeX Citations",
        "icon": "$(book)"
      },
      {
        "command": "latex-citation-stats.copyCitationKey",
        "title": "Copy Citation Key",
        "category": "LaTeX Citations",
        "icon": "$(copy)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "latex-citation-stats.refresh",
          "when": "view == latexCitationStats.view",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "latex-citation-stats.goToUsage",
          "when": "view == latexCitationStats.view && viewItem =~ /^citationInstance/",
          "group": "navigation@1"
        },
        {
          "command": "latex-citation-stats.goToBibDefinition",
          "when": "view == latexCitationStats.view && viewItem =~ /^citationEntry$|^citationEntryUnused$|^citationInstance$/",
          "group": "navigation@2"
        },
        {
          "command": "latex-citation-stats.copyCitationKey",
          "when": "view == latexCitationStats.view && viewItem =~ /^citation(Entry|Instance|UndefinedKey)/",
          "group": "9_cutcopypaste@1"
        }
      ],
      "commandPalette": [
        {
          "command": "latex-citation-stats.goToUsage",
          "when": "false"
        },
        {
          "command": "latex-citation-stats.goToBibDefinition",
          "when": "false"
        },
        {
          "command": "latex-citation-stats.copyCitationKey",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "LaTeX Citation Stats",
      "properties": {
        "latex-citation-stats.debounceDelay": {
          "type": "number",
          "default": 250,
          "minimum": 0,
          "markdownDescription": "Delay in milliseconds before re-parsing a document after you stop typing. Higher values reduce CPU usage while typing; lower values feel more instant."
        },
        "latex-citation-stats.showOverview": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show the **Overview** node with total, used, unused, and citation counts at the top of the Citations view."
        },
        "latex-citation-stats.sortOrder": {
          "type": "string",
          "default": "usage",
          "enum": [
            "usage",
            "alphabetical"
          ],
          "enumDescriptions": [
            "Most-cited sources first, unused last.",
            "Sort sources by citation key (A–Z)."
          ],
          "markdownDescription": "How to order the sources listed in the Citations view."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "bun run package",
    "compile": "bun run check-types && bun run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "bun run check-types && bun run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "bun run compile-tests && bun run compile && bun run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test",
    "vsce:package": "bunx @vscode/vsce package --no-dependencies",
    "vsce:publish": "bunx @vscode/vsce publish --no-dependencies",
    "vsce:login": "bunx @vscode/vsce login",
    "vsce:verify": "bunx @vscode/vsce verify-pat",
    "ovsx:publish": "bunx ovsx publish --no-dependencies"
  },
  "devDependencies": {
    "@types/vscode": "^1.125.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "typescript-eslint": "^8.61.1",
    "eslint": "^10.5.0",
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
